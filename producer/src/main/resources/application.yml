spring:
  application:
    name: app-producer
server:
  port: 8001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka/,http://localhost:8200/eureka/

    register-with-eureka: true
    fetch-registry: true

